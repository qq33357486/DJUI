# 更新日志

## [0.7.4] - 2026-08-01

### 优化
- 拖动控件落到「父节点本体」上（即想放进某个容器）时，智能区分两种语义：
  - **同父移动**（拖动节点原本就是该容器的子节点）→ 插到子列表**头部**（换位置语义）
  - **跨父移入**（拖动节点来自别的容器）→ 追加到子列表**末尾**（外到内语义）
  - 修复原先无论何种情况都 `push` 到末尾、导致同父节点想换到首位却被甩到最后的问题。

## [0.7.3] - 2026-08-01

### 修复
- 彻底修复左侧图层树「把控件拖到列表第一个位置会被甩到末尾」的问题。根因（上游缺陷）：`rc-tree` 的 `calcDropPosition` 中，「拖到列表最前」对应的关键分支 `dropPosition = -1` 带 `level === 0` 门禁——仅对顶层节点生效；而 DJUI 的控件都位于页面节点之下（level >= 1），永远走不到该分支，导致 `info.node` 回退成前一节点、`dropPosition` 被误判为 after，最终插到末尾。上一版（0.7.1）只改了 DJUI 侧的解析公式，无法弥补上游残缺的输入信号。
  - 本次修复彻底弃用 `info.dropPosition`/`info.node` 的歧义语义：在 `buildControlTree` 给每个节点打 `data-node-key`，drop 时从鼠标实际悬停的 DOM 反查真实 target；并用鼠标 Y 坐标 vs 目标节点边界的三分法直接判定 before/after/inside，与 rc-tree 内部怪癖完全解耦。

## [0.7.2] - 2026-08-01

### 优化
- 进度条模板的 `背景` 与 `滑块` 子节点默认加上 `stretch: Both`（四边距为 0），自动跟随外层进度条容器尺寸拉伸。调整进度条容器宽高时无需再手动改子节点尺寸，使用更方便。

## [0.7.1] - 2026-08-01

### 修复
- 修复左侧图层树拖拽排序「无法把控件移到第一个位置，拖到 0 号位反而跑到容器末尾」的问题。根因：antd Tree 的 `info.dropPosition` 是「绝对位置序号」（内部相对值 + target 在父节点中的索引），而旧代码误把它当作 -1/0/1 的「相对前后标志」直接判断 before/after，导致落点判定错乱——尤其在拖向容器内第一个子节点时被误判为 `inside`（成为子节点），再经 store 的 `push` 追加到末尾，表现为「跑到最后一个」。修复改用 antd 官方推荐的 `info.node.pos` 反推相对落点（`dropPosition - targetIndex`）。同时修正 `moveNode` 中 `'root'` 魔法字符串与真实 `page.root.id` 不匹配的隐患：`findNode(root, 'root')` 恒为 null，曾导致「拖到页面节点上」时控件从旧位置删除后新位置插入被静默跳过（控件消失）。

## [0.7.0] - 2026-07-30

### 破坏性变更
- **进度条控件结构重构**：从「控件本身就是滑块图」改为 NGUI 风格的「外层 Panel 容器 + 背景/滑块双子节点」。新建进度条时自动生成三个节点：外层 `进度条`(Panel 容器) → 子节点 `背景`(Panel，贴背景图) + `滑块`(Progress，贴填充图并按 value 裁剪)。线性和圆形进度条统一复用引擎 Progress 类的裁剪能力。旧的「光杆 Progress」JSON 仍可加载渲染，只是没有独立背景层。

### 优化
- 进度条的「方向/进度/起始角」属性现在作用于内层 `滑块` 子节点（选中滑块时在右侧面板编辑），背景图在 `背景` 子节点上编辑，职责清晰、互不干扰。
- 新建进度条的 `背景` 与 `滑块` 子节点默认使用 `Both` 拉伸并保持四边距为 0；调整外层进度条尺寸时，两个子节点自动同步。
- 移除了右侧面板「进度条用 Image 做前景遮罩，背景需另加 Panel 放后面」的过时提示。
- `createNode` 现支持模板内带子节点（子节点 id 用占位符，克隆时递归重分配，保证多实例唯一）。

## [0.6.6] - 2026-07-30

### 优化
- 增强画布辅助线（素材边框）可见度：加粗并提亮，选中和非选中状态差异更明显。普通控件未选中边框由暗灰蓝 `#3a4258`/1px 改为亮灰蓝 `#7a8aa8`/1.5px，选中态由 2px 加粗至 2.5px（蓝 `#5ab9ff`）；模板实例边框同步提亮加粗（未选中紫 `#8a6fc0`/1.5px、选中 `#b37feb`/2.5px）。

## [0.6.5] - 2026-07-29

### 修复
- 修复左侧图层树「窗口名过长遮挡删除按钮、无法点击删除」的问题。根因：页面名 span 用 `flex:1` 无限扩张且缺少 `minWidth:0`，文本省略号未生效，把右侧删除按钮挤出可视区。修复：name span 与外层容器补 `minWidth:0` 触发省略号，删除按钮包入 `flexShrink:0` 的固定容器（与控件节点按钮组布局一致），超长名称以省略号截断，删除按钮始终可见可点。

### 新增
- Delete 键删除页面：未选中控件时按 Delete 删除当前活动页面（带二次确认，防误删）。删除控件的 Delete/Backspace 逻辑保持原有（画布全局监听），两者按「是否选中控件」自动分工，互不冲突。

## [0.6.4] - 2026-07-29

### 修复
- 修复「打开辅助线后，透明背景控件被实心深色填充盖住下层素材」的问题。原实现对透明背景控件画了 `#1e2a3a`（注释误称半透明，实际为完全不透明）的底色，导致位于其下方的素材被遮挡。现改为透明背景控件不再画任何填充，仅保留边框线（stroke）作为辅助标识。边框/选中描边/锚点可视化等其它辅助线不受影响。

## [0.6.3] - 2026-07-29

### 新增
- **控件锁定宽高比**：右侧「位置尺寸」区宽/高输入框之间新增锁链开关（🔒/🔓）。开启后画布拖拽缩放手柄与右侧宽/高输入均按当前 W:H 比例联动调整（Figma/Photoshop 直觉）。比例取锁定时刻的当前宽高比，不额外存储固定比例字段，与现有运行期 `aspectRatio`（uGUI AspectRatioFitter）约束互不干扰。
  - 拉伸轴（Stretch）下尺寸由父容器决定，锁形开关自动禁用并提示。
  - 画布缩放仍保留 Shift 临时等比（未开锁形时按住 Shift 也能等比缩放）。

## [0.6.2] - 2026-07-29

### 修复
- 修复「在左侧图层树点击非当前页面的控件时，控件被选中但画布不跳转」的问题。根因：`handleSelect` 点击控件时只调用 `selectNode`，未切换 `activePageId`，而画布始终渲染当前活动页面，导致看不到选中效果。修复：点击控件时先查找其所属页面（`findPageIdOfNode`），若非当前页面则 `await onSwitchPage` 切换画布后再选中（用 await 保证页面切换的清空选中动作先于 `selectNode` 完成，选中不会被误清）。

## [0.6.1] - 2026-07-29

### 修复
- 修复「多个页面里同名控件（如多个页面都有『背景』）在左侧图层树同时显示选中」的问题。根因有二：
  - **选中态缺页面作用域**：`selectedIds` 是全局数组按 id 匹配，左侧树渲染所有页面节点时，非当前页面的节点只要 id 命中也会高亮。修复：`buildControlTree` 增加 `pageId`/`activePageId` 作用域判断，选中态只作用于当前活动页面。
  - **跨页面 id 撞车**：通过复制 JSON 文件等方式创建的页面，其内部控件 id 可能与其它页面相同；加载时未做去重。修复：新增 `dedupeNodeIdsAcrossPages`，在 `setAllPages`/`upsertPage` 加载时保证 `allPages` 内所有节点 id 跨页面唯一（冲突 id 重分配为 `${pageId}_${原id}_${counter}`）。

## [0.6.0] - 2026-07-29

### 优化
- **画布选中/拖动交互重构**：解决「选中父节点后，被更大的子节点盖住时无法拖动父节点」的痛点。现在按下→弹起无位移才算「点击选中」；若按下后发生拖动，则拖动当前已选中的节点（即便它被更大的子节点完全遮挡），符合 Figma/Sketch 的操作直觉。
  - 引入「拖动捕获层（Proxy）」：为每个已选中节点在最上层叠一层透明命中区，使「按住已选父节点拖动」能穿透更大的子节点。
  - 引入指针会话（pointer session）统一管理按下→拖动→弹起，用 4px 阈值区分「点击」与「拖动」，避免按下即改变选中。
  - 一次按住即可拖动未选中节点（拖动开始时自动选中它）。
  - 触摸设备沿用「tap 即选中」的简单模型，行为不退化。

## [0.5.4] - 2026-07-05

### 修复
- 修复保存页面无任何提示/反馈的问题（点击保存按钮、Ctrl+S 均无效）。根因：zustand + immer middleware 会冻结 store 中的 `page` 对象，而 patches 保存流程中 `delete` 属性操作在冻结对象上抛 `Cannot delete property 'version'`，导致 `message.success` 永不执行。修复：在 `patchAndSavePage` 入口深拷贝页面数据，patches 系统在可变副本上工作，原 store 状态不受影响。

## [0.5.3] - 2026-07-03

### 新增
- 颜色选择器新增屏幕取色管（EyeDropper），可拾取屏幕任意位置颜色；不支持该 API 的浏览器（如 Firefox）点击时给出提示。

## [0.5.2] - 2026-07-03

### 新增
- 文本控件支持文字描边配置，普通控件支持单色边框配置，并同步到 Runtime 渲染。

## [0.5.1] - 2026-07-03

### 优化
- 按钮默认音效未配置时仅首次弹出说明，后续改为顶部栏非打断提醒。

## [0.5.0] - 2026-07-03

### 新增
- 颜色与透明度拆分为独立字段，遵循 PS 习惯（0%=全透明 100%=可见）
- 图片控件支持独立的图片透明度调节

### 优化
- 属性面板 UX 重组：外观与文本拆分，锚点与拉伸默认展开
- 整体透明度显示改为百分比（0-100%），存储仍为 0-1 兼容 Runtime

### 修复
- 修复颜色选择器在 alpha=0 时选新颜色强制改为不透明的问题
- 修复有图片时调整背景透明度无效的问题（改为绑定图片透明度）

## [0.4.0] - 2026-07-03

### 新增
- 自适应容器（auto-size）布局：容器可按子控件边界自动调整宽高
- 更新公告功能：编辑器版本升级时自动弹出时间线式变更日志
- 数据边界防护体系：normalize 归一化层 + AppErrorBoundary 防白屏

### 修复
- 修复老项目加载白屏问题（节点缺少 children 字段导致渲染崩溃）
- 修复锚点拉伸轴拖拽时坐标换算错误

### 优化
- 重构拖拽系统：支持父子节点联动预览，拖拽体验更流畅
- 纯前端架构迁移完成，移除后端服务

## [0.3.0] - 2026-06-29

### 新增
- 纯前端架构：使用 File System Access API 直接读写工程目录
- 声音配置系统：支持按钮点击音效绑定
- 九宫格切片编辑器
- AGENTS.md 规范自动更新机制
- 脚本区同步功能

### 优化
- 锚点系统重构为 NGUI 风格（anchor.side 9-way）
- Runtime 同步改用前端 File System Access API

## [0.2.0] - 2026-06-15

### 新增
- Docker 容器化部署
- 在线实例 https://ui.duojie.games
- GHCR 镜像发布

## [0.1.0] - 2026-06-10

### 新增
- 初始版本：Web Editor + C# Runtime + 素材处理脚本
- 可视化 UI 页面编辑（拖拽、属性面板、画布预览）
- JSON 导出 + Runtime 控件树构建

# 更新日志

## [0.16.4] - 2026-08-19

### 修复
- 修复网页里更新 Runtime、走完发布流程后，AI 命令行发布仍报「Runtime 版本不符」的问题：Runtime 与本地发布器现在绑定为同一版本，只要 Runtime 更新了，网页就会提醒同步脚本区。
- 在网页更新 Runtime 后，如果脚本区尚未同步，会立即弹出提醒，不必等到 AI 发布失败才发现。

### 优化
- AI 发布命令检测到星火工程的 Runtime 比自身新时，会明确提示先在网页同步脚本区，不再引导执行会把 Runtime 降回旧版的操作。
- 「脚本区已是最新」的判定同时核对发布器文件本身，即使版本标记遗漏也能发现需要更新。

## [0.16.3] - 2026-08-19

### 新增
- 按钮支持悬停、按下、禁用三种状态图，属性面板新增「按钮状态」分组，附四态缩略预览，可一目了然看清各状态效果。
- 按钮禁用后终于有明显的视觉反馈了：配置禁用图时显示禁用图；没配置时自动变灰并变淡，无需额外准备素材。
- 画布上禁用中的按钮会直接显示禁用效果，并带「禁」角标，一眼区分可点与不可点。
- 运行时可通过数据绑定动态切换按钮禁用状态（绑定属性 disabled），例如金币不足时置灰购买按钮。
- 游戏代码可调用 DjuiButtonState.SetDisabled() 动态禁用/恢复按钮，视觉自动同步刷新。

### 修复
- 修复按钮悬停图、按下图在运行时始终不生效的问题（此前配置了也不会切换）。
- 修复选中嵌套在卡片内的控件时，锚点标记错位到画布左上角的问题。

### 优化
- Runtime 升级到 0.7.17，按钮状态与禁用视觉需要更新 Runtime 后生效。
- 修正 UI 工作区规范文档中「待审核面板」的错误描述：素材审批一直是直接在工作区目录里人工移动文件，编辑器没有审批界面。
- 重做选中控件时的锚点辅助显示：不再画父容器虚线大框，改为锚点参考线 + 锚点到中心点的外延线，锚点位置带「锚点」文字标注；未设置锚点的控件选中时不再显示任何参考元素。

## [0.16.2] - 2026-08-19

### 优化
- 画布的辅助线和效果图显示开关现在会记住上次选择，刷新编辑器后不再总是自动打开。

## [0.16.1] - 2026-08-19

### 修复
- 更新脚本区或通过 AI 发布旧项目时，现在会自动把遗留在星火工程内的页面、项目配置和音效迁入 UI 工作区，不再要求手工搬迁。

## [0.16.0] - 2026-08-19

### 新增
- UI 工作区现在提供 Node 发布命令，AI 完成素材或页面修改后无需打开网页即可发布到已配置的星火工程。
- 发布命令会先检查 Runtime；需要更新时会明确返回提示，确认后可单独执行 Runtime 更新再发布。

### 优化
- 网页发布与 AI 命令行发布现在共用同一套规则和校验，避免两种方式的发布结果不一致。

## [0.15.5] - 2026-08-19

### 新增
- 窗口页可指定另一个窗口页作为编辑器后景，设计前景 UI 时可直接查看场景内容的完整合成效果。

## [0.15.4] - 2026-08-19

### 优化
- 适配审计页现在保留页面目录，可直接切换页面并立即查看新的审计结果。
- 设备风险明细移至右侧，设备预览与审计结果的对应关系更清晰。
- 从审计结果回到画布时，会保留刚刚查看的设备画像，方便继续复核。

## [0.15.3] - 2026-08-18

### 修复
- 修复首次载入工程时画布可能触发渲染错误的问题。

## [0.15.2] - 2026-08-18

### 修复
- 连续输入、滑条和数值拖拽现在只会在操作结束后记为一次撤销，不再占用多条历史记录。
- 切换锚点、锚定目标或安全边时保持控件视觉位置不变；多选时所有选中控件一起正确换算。

## [0.15.1] - 2026-08-18

### 新增
- 支持 Ctrl/Cmd+Z 撤销、Ctrl/Cmd+Shift+Z 与 Ctrl+Y 重做；整个 UI 工程统一保留最近 100 步操作。

### 修复
- 修复多选控件整体拖动时只有部分控件移动的问题；不同锚点和拉伸规则的控件现在会一起移动。

## [0.15.0] - 2026-08-18

### 新增
- 新建工程时可选择竖屏 9:20 或横屏 20:9；创建后方向锁定，设备预览只显示同方向画像。
- 适配审计改为左侧风险清单、右侧全部设备画布预览，可同时检查手机、平板、刘海、挖孔、曲面边缘和手势区。

### 修复
- 修复同为 9:20 的标准机、挖孔屏和曲面屏可能互相套用安全区的问题；现在每种硬件画像独立生效。

## [0.14.0] - 2026-08-18

### 新增
- 「视图」菜单新增独立的适配审计页面，集中查看全部设备画像与风险明细，不再遮挡目录和画布。

### 优化
- UI 工作区成为项目配置、页面和音效的唯一编辑源；发布时再同步到星火工程，避免直接在游戏工程中改 UI 源文件。

## [0.13.0] - 2026-08-18

### 修复
- 场景建筑、地图标记等素材坐标内容会随背景图帧同步缩放；切换屏幕比例后不再只移动外层、内部位置和大小却失配。

### 优化
- 场景明确指定所依附的背景和素材画幅，编辑器、发布校验与游戏运行使用同一套坐标规则；错误配置会在发布前拦截。

## [0.12.0] - 2026-08-18

### 新增
- 新建工程固定使用 9:20 竖版基准，横版 20:9 纳入设备画像预览，不再要求先选择分辨率模板。
- 画布设备预览新增灵动岛、摄像孔、曲面屏、手势区、折叠屏与横屏平板画像，并可一次扫描全部画像的适配风险。
- 选择设备画像时自动检查交互控件是否越界、落入系统安全区外、撞到手势/曲面触控区或点击尺寸过小。

## [0.11.0] - 2026-08-17

### 新增
- 新增「背景图帧锚定」：场景建筑等需要固定在背景图上的内容，切换任何屏幕比例都与背景保持原有相对位置，不再随屏幕裁切漂移。

## [0.10.1] - 2026-08-17

### 优化
- 发布提速：素材改用增量同步，内容未变的文件直接跳过，素材越多提速越明显（无变化时约为原耗时 1/7）；发布完成提示会显示复制/跳过/清理的数量。
- 新增本地校验命令：改完页面 JSON 后可在命令行直接校验（与编辑器同一套规则），不用打开编辑器发布才能发现配置问题。

## [0.10.0] - 2026-08-17

### 新增
- 「文件」菜单新增「最近打开的工程」：像 VS Code 一样一键切回最近用过的工程，无需重新选目录；每个工程记忆各自的目录授权。

### 修复
- 修复通过「打开工程」切换工程后界面仍显示旧工程内容、必须手动刷新页面的问题（切换工程后自动整页刷新）。

## [0.9.9] - 2026-08-17

### 新增
- 新增字体管理：可导入自定义字体文件（.ttf / .otf / .ttc），画布与游戏引擎使用同一字体文件渲染，效果完全一致。
- 字体下拉按「完全一致 / 近似预览 / 不可用」分组显示，未选字体时统一显示「引擎默认字体」。
- 工作区规范文档新增全屏素材专项规范（竖版/宽版两套画幅、构图安全区、验收方法）与字体使用规范。
- Runtime 部署说明新增响应式宽屏层（宽屏判定规则、可覆盖字段、全屏背景双节点换图范式）与字体章节。

### 修复
- 修复竖屏手机被误判为宽屏档、导致宽屏专属素材在竖屏显示的问题。
- 修复编辑器画布中文长文本不断行、超出后直接被裁掉看不到的问题。
- 修复编辑器画布文字描边盖住笔画、描边后字看不清的问题。
- 修复编辑器画布忽略控件隐藏设置、隐藏的宽屏背景照常显示并盖住竖屏背景的问题。
- 修复切换预览分辨率后画布适配缩放按错误尺寸计算、画布溢出编辑器视口的问题。

### 优化
- Runtime 升级到 0.7.9，宽屏档判定改为方向感知：只有画面实际比手机竖屏更宽时才启用宽屏适配。
- 编辑器画布支持系统字体（如微软雅黑），画布文字与引擎运行画面使用同一款字体。
- 引擎封装格式字体在画布上明确提示使用回退字体，不再静默失败。

## [0.9.8] - 2026-08-17

### 修复
- 修复发布后运行时缺少 v6 项目适配配置、导致页面无法启动的问题。
- 修复严格页面加载遇到安全区和九宫格图片配置时可能失败的问题。
- 修复多层嵌套控件在运行时重复计算父级偏移、越嵌套越偏的问题。
- 修复绝对布局控件被边距或对齐设置再次偏移的问题。

### 优化
- Runtime 升级到 0.7.8，完善项目配置发布、九宫格显示、嵌套布局与编辑器预览一致性。

## [0.9.7] - 2026-08-17

### 修复
- 修复 Runtime 0.7.0/0.7.1 在星火工程中安装后可能无法编译触摸反馈的问题。
- 修复设备旋转或反复调整窗口后按钮反馈可能重复叠加的问题。
- 修复关闭窗口后持续动效和数据绑定可能残留的问题。
- 修复同一页面打开多个实例时，相同节点 ID 可能互相覆盖的问题。

### 优化
- Runtime 升级到 0.7.0，窗口关闭转场完成后再安全释放，关闭过程中重新打开可保留原控件状态。
- 完善拖放、触摸反馈、边距、内边距和内容对齐等页面属性的运行效果。

## [0.9.6] - 2026-08-16

### 新增
- 新增 DJUI v6 通用响应式布局：同一套页面可适配手机、长屏、折叠屏和宽屏设备。
- 画布提供基础层与宽屏层编辑，可为宽屏单独调整允许覆盖的属性并实时预览。
- 新增父节点、屏幕和安全区锚点，安全区可独立选择左、上、右、下边。
- 项目可选择完整容纳、宽度优先或高度优先，并可配置宽屏触发比例。
- Runtime 图片支持 stretch、contain、cover 与焦点定位，窗口尺寸变化后会原地更新显示。
- 页面模板支持嵌套、实例隔离和模板参数覆盖。

### 修复
- 修复保存工程设置时可能重置适配模式和宽屏阈值的问题。
- 修复保存页面时可能清空已有宽屏覆盖的问题。
- 修复嵌套控件在运行时出现位置偏移的问题。
- 修复按钮图片适配时可能影响文字和子控件层级的问题，并支持运行中移除图片。

### 破坏性变更
- v6 页面使用严格协议校验；旧版页面必须先迁移，编辑器不会再静默改写旧文件。

## [0.9.5] - 2026-08-16

### 修复
- 页面与音效改为只发布到客户端目录（ui/AppBundle）——服务端本来就不读取它们，之前往服务端目录也写一份纯属多余。

## [0.9.4] - 2026-08-16

### 修复
- 修复发布后游戏里页面不生效的问题：发布现在会写入客户端页面目录（ui/AppBundle），不再需要手动拷贝。

### 新增
- Runtime 升级到 0.4.0：随安装附带部署说明文档（AGENTS.md），页面目录缺失或页面打不开时给出明确的修复指引，不再只是无声失败。

## [0.9.3] - 2026-08-16

### 优化
- 素材加工改为按类型限制最大边并保留原始比例；只有明确需要时才创建固定方形画布。

## [0.9.2] - 2026-08-01

### 修复
- 再次修复刷新后字体选择列表为空的问题（改为工程加载时集中读取字体列表存入全局状态，不再依赖各组件自己的加载时机）。

## [0.9.1] - 2026-08-01

### 修复
- 修复刷新页面后字体选择列表为空的问题（刷新时工程目录 handle 还没恢复就读字体，导致读不到）。

## [0.9.0] - 2026-08-01

### 新增
- 画布现在能显示引擎实际的字体：打开工程时自动把字体文件注册到浏览器，所见即所得。
- 新建文字控件会自动使用当前的全局默认字体，无需逐个设置。

### 优化
- 设置全局默认字体时，会同步应用到所有已有的文字控件，导出的数据每个控件都明确指向字体。

## [0.8.2] - 2026-08-01

### 修复
- 修复多选拖动箭头出现在错误位置的问题（箭头未跟随画布缩放和平移）。
- 修复用箭头拖动多选控件后，松开鼠标位置又回到原处的问题。

## [0.8.1] - 2026-08-01

### 优化
- 多选交互改为 Excel 习惯：默认点击单选，按住 Ctrl 单点追加/取消，按住 Shift 连续范围选择，选中其它时自动取消多选。

## [0.8.0] - 2026-08-01

### 新增
- 图层树和画布支持批量多选控件（限同一个容器内的兄弟节点），多选后画布出现 Unity 风格的红蓝双色 XY 轴箭头，拖动箭头可整组移动选中控件的位置。

## [0.7.6] - 2026-08-01

### 修复
- 修复图层树拖拽换位置后控件坐标发生偏移的问题。
- 修复拖拽指示线颜色不随落点类型变化的问题。

## [0.7.5] - 2026-08-01

### 优化
- 图层树拖拽时，指示线按落点区分颜色：放入子节点显示橙色、交换位置显示蓝色。

## [0.7.4] - 2026-08-01

### 优化
- 拖动控件到容器上时智能判断：同一容器内换位置插到最前，跨容器移入则追加到最后。

## [0.7.3] - 2026-08-01

### 修复
- 修复左侧图层树把控件拖到列表第一个位置时被甩到末尾的问题。

## [0.7.2] - 2026-08-01

### 优化
- 进度条的背景和滑块子节点自动跟随容器尺寸拉伸。

## [0.7.1] - 2026-08-01

### 修复
- 修复左侧图层树拖拽排序时无法把控件移到第一个位置的问题。

## [0.7.0] - 2026-07-30

### 破坏性变更
- **进度条控件结构重构**：新建的进度条现在是「外层容器 + 背景 + 滑块」三个节点，背景图和滑块图分别管理，线性和圆形都支持。旧页面里的进度条仍可正常打开。

### 优化
- 进度条的背景图和滑块图现在分开编辑，互不干扰。

## [0.6.6] - 2026-07-30

### 优化
- 增强画布辅助线（素材边框）可见度：加粗提亮，选中和非选中状态差异更明显。

## [0.6.5] - 2026-07-29

### 修复
- 修复左侧图层树窗口名过长时遮挡删除按钮、无法点击删除的问题。

### 新增
- Delete 键删除页面：未选中控件时按 Delete 删除当前页面（带二次确认）。

## [0.6.4] - 2026-07-29

### 修复
- 修复打开辅助线后，透明背景控件被实心深色填充盖住下层素材的问题。

## [0.6.3] - 2026-07-29

### 新增
- 控件锁定宽高比：右侧「位置尺寸」区宽/高输入框之间新增锁链开关，开启后拖拽缩放和输入宽高都按当前比例联动。

## [0.6.2] - 2026-07-29

### 修复
- 修复在左侧图层树点击其它页面控件时，控件被选中但画布不跳转的问题。

## [0.6.1] - 2026-07-29

### 修复
- 修复多个页面里有同名控件时，点击其中一个其它页面的同名控件也被误显示为选中的问题。

## [0.6.0] - 2026-07-29

### 优化
- **画布选中/拖动交互重构**：解决「选中父节点后，被更大的子节点盖住时无法拖动父节点」的痛点。现在按下→弹起无位移才算「点击选中」；若按下后发生拖动，则拖动当前已选中的节点（即便它被更大的子节点完全遮挡），符合 Figma/Sketch 的操作直觉。
  - 引入「拖动捕获层（Proxy）」：为每个已选中节点在最上层叠一层透明命中区，使「按住已选父节点拖动」能穿透更大的子节点。
  - 引入指针会话（pointer session）统一管理按下→拖动→弹起，用 4px 阈值区分「点击」与「拖动」，避免按下即改变选中。
  - 一次按住即可拖动未选中节点（拖动开始时自动选中它）。
  - 触摸设备沿用「tap 即选中」的简单模型，行为不退化。

## [0.5.4] - 2026-07-05

### 修复
- 修复保存页面无任何提示/反馈的问题（点击保存按钮、Ctrl+S 均无效）。根因：zustand + immer middleware 会冻结 store 中的 `page` 对象，而 patches 保存流程中 `delete` 属性操作在冻结对象上抛 `Cannot delete property 'version'`，导致 `message.success` 永不执行。修复：在 `patchAndSavePage` 入口深拷贝页面数据，patches 系统在可变副本上工作，原 store 状态不受影响。

## [0.5.3] - 2026-07-03

### 新增
- 颜色选择器新增屏幕取色管（EyeDropper），可拾取屏幕任意位置颜色；不支持该 API 的浏览器（如 Firefox）点击时给出提示。

## [0.5.2] - 2026-07-03

### 新增
- 文本控件支持文字描边配置，普通控件支持单色边框配置，并同步到 Runtime 渲染。

## [0.5.1] - 2026-07-03

### 优化
- 按钮默认音效未配置时仅首次弹出说明，后续改为顶部栏非打断提醒。

## [0.5.0] - 2026-07-03

### 新增
- 颜色与透明度拆分为独立字段，遵循 PS 习惯（0%=全透明 100%=可见）
- 图片控件支持独立的图片透明度调节

### 优化
- 属性面板 UX 重组：外观与文本拆分，锚点与拉伸默认展开
- 整体透明度显示改为百分比（0-100%），存储仍为 0-1 兼容 Runtime

### 修复
- 修复颜色选择器在 alpha=0 时选新颜色强制改为不透明的问题
- 修复有图片时调整背景透明度无效的问题（改为绑定图片透明度）

## [0.4.0] - 2026-07-03

### 新增
- 自适应容器（auto-size）布局：容器可按子控件边界自动调整宽高
- 更新公告功能：编辑器版本升级时自动弹出时间线式变更日志
- 数据边界防护体系：normalize 归一化层 + AppErrorBoundary 防白屏

### 修复
- 修复老项目加载白屏问题（节点缺少 children 字段导致渲染崩溃）
- 修复锚点拉伸轴拖拽时坐标换算错误

### 优化
- 重构拖拽系统：支持父子节点联动预览，拖拽体验更流畅
- 纯前端架构迁移完成，移除后端服务

## [0.3.0] - 2026-06-29

### 新增
- 纯前端架构：使用 File System Access API 直接读写工程目录
- 声音配置系统：支持按钮点击音效绑定
- 九宫格切片编辑器
- AGENTS.md 规范自动更新机制
- 脚本区同步功能

### 优化
- 锚点系统重构为 NGUI 风格（anchor.side 9-way）
- Runtime 同步改用前端 File System Access API

## [0.2.0] - 2026-06-15

### 新增
- Docker 容器化部署
- 在线实例 https://ui.duojie.games
- GHCR 镜像发布

## [0.1.0] - 2026-06-10

### 新增
- 初始版本：Web Editor + C# Runtime + 素材处理脚本
- 可视化 UI 页面编辑（拖拽、属性面板、画布预览）
- JSON 导出 + Runtime 控件树构建

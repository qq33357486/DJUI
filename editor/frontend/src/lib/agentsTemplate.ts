// AGENTS.md 模板（从后端移植，前端唯一权威定义）

export const AGENTS_VERSION = '0.7.4'

export const AGENTS_VERSION_TAG_PREFIX = '<!-- DJUI-AGENTS-VERSION:'
export const AGENTS_VERSION_TAG_SUFFIX = ' -->'

function escapeRegex(s: string): string {
  return s.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')
}

export function readAgentsVersion(fileContent: string): string | null {
  const m = fileContent.match(
    new RegExp(`${escapeRegex(AGENTS_VERSION_TAG_PREFIX)}\\s*([0-9A-Za-z.\\-]+)\\s*${escapeRegex(AGENTS_VERSION_TAG_SUFFIX)}`)
  )
  return m ? m[1] : null
}

export function buildVersionTag(version: string = AGENTS_VERSION): string {
  return `${AGENTS_VERSION_TAG_PREFIX} ${version}${AGENTS_VERSION_TAG_SUFFIX}`
}

export function buildAgentsMd(): string {
  const versionTag = buildVersionTag()
  return `${versionTag}
# DJUI UI 工作区

此目录是 DJUI 编辑器的 UI 工作区，存放 UI 配置和 UI 图素材。
请自行备份好此目录。

> 本文件由 DJUI Editor 自动生成。当编辑器内置规范升级后，菜单栏的「帮助」会提示更新。
> **请勿手动编辑** — 你对素材目录的整理是有效的，但本文件的结构描述以编辑器为准。

---

## 一、目录结构

\`\`\`
工作区根目录/
├── AGENTS.md          # 本文件（编辑器自动维护）
├── .djui/layout/      # UI 项目配置源：project.json、sounds.json、pages/
├── 原始素材/          # 原图、设计稿、AI 生图（输入素材，不参与发布）
│   └── YYYY-MM-DD/    # 按日期分目录，可再按主题/批次建子目录
├── 成品素材/          # 人工审批后的最终素材，「发布」时同步到星火工程
│   ├── backgrounds/   # 背景图
│   ├── buttons/       # 按钮
│   ├── frames/        # 边框、对话框底框
│   ├── icons/         # 图标
│   ├── lists/         # 列表项、卡片
│   ├── decorations/   # 装饰物、特效、花纹
│   ├── text/          # 艺术字、标题图
│   └── misc/          # 未分类
├── 临时文件/          # 中间产物、实验稿，可随时清理
│   ├── 去绿幕后/      # 去绿幕脚本输出
│   └── 待审核/        # 裁边压缩后、等待人工审批的素材（按分类子目录）
│       ├── icons/
│       ├── buttons/
│       └── ...
├── 文档/              # 设计文档、标注
└── 脚本区/            # 工具脚本（去绿幕、裁边、压缩等，由 DJUI 维护）
\`\`\`

---

## 二、原始素材规范

- **按日期管理**：每天创建一个日期目录，如 \`原始素材/2026-06-28/\`
- **可再细分**：日期目录下可按主题/批次建子目录，如 \`原始素材/2026-06-28/home_bg/\`
- 放入内容：原图、AI 生图、截图、参考图、PSD 等
- 这些素材**不参与发布**，仅作为加工来源
- 推荐保留原始高分辨率，压缩版归到 \`临时文件/\` 或丢弃

---

## 三、移动端素材尺寸规范（重要）

本项目目标平台是**手机游戏**。所有成品素材的最终分辨率必须遵守下面规则。
依据 Apple Human Interface Guidelines 和 Google Material Design 的移动应用贴图建议：

### 3.1 硬性上限

| 项 | 上限 | 说明 |
|---|---|---|
| 普通 UI 贴图默认最大边长 | **1024 px** | 图标、按钮、边框、卡片和可平铺/九宫格背景默认遵守；完整构图全屏插画按下方专项规范 |
| 单文件大小 | **500 KB** | 普通 UI 素材目标值；全屏插画建议压缩后不超过 2–4 MB，并按真机内存预算复核 |
| 2 的幂对齐 | 推荐 | 1024/512/256/128/64/32；非 2 的幂 PNG 也能用但纹理上传略慢 |

### 3.2 各类素材默认最大边

以下数值是默认**最大边**，不是把所有素材补透明区后强行做成固定尺寸。加工时先 alpha 紧裁、保留原始宽高比，只在超过最大边时等比缩小；只有明确要求统一画布的独立图标，才额外指定固定宽高和 padding。

| 素材类型 | 推荐尺寸 | 说明 |
|---|---|---|
| 图标（icons） | **64 / 128 / 256** | 功能小图标用 64 或 128；大图标最多 256；需要方形画布时单独声明 |
| 按钮（buttons） | **最大边 512** | 保留横长或方形按钮原比例；按控件需求做九宫格 |
| 列表项/卡片（lists） | **最大边 512** | 保留卡片原比例，可拉伸的留 9 宫格安全区 |
| 边框（frames） | **最大边 512** | 九宫格切片框保留原比例 |
| 装饰物（decorations） | **最大边 512** | 插画、光效、花纹、角标保留原比例 |
| 艺术字（text） | **最大边 512** | 标题图、Logo 保留原比例 |
| 可平铺/九宫格背景（backgrounds） | **最大边 1024** | 无完整构图的底纹、渐变和分区背景优先平铺、九宫格或程序填色 |
| 完整构图全屏插画（backgrounds） | **竖版 1440×3200 母版，建议成品 1080×2400；宽版 2560×1600 母版，建议成品 1920×1200** | 启动图、登录图、完整场景插画；至少竖版和宽版两档，不得用一张图强行覆盖手机与横屏平板 |
| 未分类（misc） | **最大边 512** | 默认保留原比例 |

### 3.3 同一资源多尺寸

需要在不同设备密度下显示的图标/头像，输出多个尺寸版本，用尺寸后缀区分：

\`\`\`
icons/icon_avatar_64.png
icons/icon_avatar_128.png
icons/icon_avatar_256.png
\`\`\`

引擎侧按设备 DPR 选择加载哪个版本。

### 3.4 全屏素材专项规范（完整构图插画，重要）

适用：启动图、登录图、全屏场景插画等**铺满屏幕、有完整构图**的图。这类素材与普通 UI 贴图不同：**必须竖版 / 宽版各出一套**，并遵守构图安全区。

**画幅与尺寸**

| 套 | 设计母版 | 游戏成品 | 比例 | 覆盖设备 |
|---|---|---|---|---|
| 竖版 | 1440×3200 | 1080×2400 | 9:20 | 手机竖屏（9:16 ~ 20:9）、折叠屏展开 |
| 宽版 | 2560×1600 | 1920×1200 | 16:10 | iPad / 安卓平板横置、桌面 |

- 已有大量 9:16 存量素材的项目可继续用 1440×2560 / 1080×1920，但 20:9 设备左右各裁约 10%
- 项目明确只支持 16:9 横屏时，宽版可用 2560×1440 / 1920×1080
- **禁止**用一张图覆盖手机与平板：9:16 竖图在 16:10 平板上铺满显示时纵向只保留约 35%，主体必残
- 格式：无透明完整插画优先 WebP / JPEG；透明或锐利边缘才用 PNG。压缩后目标 2–4 MB，并按真机纹理解码内存复核

**命名（强制）**

\`\`\`
backgrounds/bg_<场景>_portrait.png   # 竖版
backgrounds/bg_<场景>_wide.png       # 宽版
\`\`\`

**构图安全区**

- 竖版：主体 / Logo / 人脸 / 关键装饰放在**中央 60% 宽 × 48% 高**内；上下各 10%、左右各 8% 只放可裁环境（天空、地面、氛围延展）
- 宽版：主体放在中央约 60% 宽内；左右各 20% 为延展区
- 折叠屏展开（近方形）会使用竖版图，纵向仅保留约 48% —— 竖版的核心主体务必压在画面纵向中段
- UI 文字、按钮、Logo 尽量做成 DJUI 文字/按钮控件叠加，**不要烘焙进背景图**

**适配行为（为什么这样出图）**

- 全屏背景控件用 \`cover\`（铺满、无黑边、可能裁切）；\`contain\`（完整显示、有留边）只用于立绘展示等需要完整构图的场合
- 焦点 \`focalX / focalY\` 默认 0.5 / 0.5（居中裁切）；主体构图偏移时按主体实际位置调整焦点
- DJUI 页面按「基础层（竖）/ 宽屏层（横）」自动切换两套图：物理宽高比 ≥ 1.25（工程设置可调）时启用宽屏层

**验收方法**

- 编辑器：画布预览切「标准手机 9:16」应四边完整；切宽屏档（如宽平板）自动换宽版图
- 引擎：星火编辑器运行界面顶部**内置分辨率按钮**逐档切换，检查裁切与清晰度

---

## 四、成品素材分类规范（重点）

成品素材是「发布到引擎」的**唯一来源**。请严格遵守：

### 4.1 总原则

1. **必须放入八大分类目录之一**，禁止直接放在 \`成品素材/\` 根目录
2. 引擎引用路径 = \`image/djui/<分类>/<可选子目录>/<文件名>\`
3. 命名：**全小写 + 下划线**，禁止中文、空格、大写字母
4. 命名前缀**必须等于分类目录名**（如 \`icons/icon_coin.png\`，不是 \`icons/coin.png\`）
5. 按钮多状态用后缀：\`_normal\` \`_pressed\` \`_disabled\` \`_hover\`
6. 尺寸必须符合 §三规范；普通 UI 默认单边 ≤ 1024，完整构图全屏插画使用专项档位
7. 普通素材目标 < 500 KB；全屏插画压缩后建议 ≤ 2–4 MB，并评估纹理解码内存
8. 格式优先级：\`.png\`（透明/需要锐利边缘）> \`.webp\`（照片/渐变）> \`.jpg\`（无透明的大背景）

### 4.2 分类决策树

拿到一张图，按下面顺序判断该放哪个分类：

\`\`\`
是图标吗？（小尺寸、单色或少量颜色、表达"功能/物品/状态"语义）
  → icons/

是按钮吗？（用于点击触发操作，可包含 normal/pressed/disabled 多状态）
  → buttons/

是文字图片吗？（艺术字、Logo 标题、装饰性数字）
  → text/

是边框/容器底框吗？（对话框、卡片外框、九宫格切片框）
  → frames/

是列表项/卡片吗？（可复用的列表行背景、商品卡、技能卡底图）
  → lists/

是装饰物吗？（光效、花纹、角标、非交互的氛围元素）
  → decorations/

是整屏背景吗？（铺满整个屏幕的底图、分区大背景）
  → backgrounds/

都不是？→ misc/（并定期重新归类）
\`\`\`

### 4.3 八大分类详细说明

| 分类目录 | 用途 | 命名模板 | 推荐尺寸 | 反例 |
|---|---|---|---|---|
| \`backgrounds/\` | 全屏背景、分区大背景、面板底图（铺满式） | \`bg_<场景>.png\` | 最大边 1024 | 把按钮塞进背景图 |
| \`buttons/\` | 可点击按钮（含多状态） | \`btn_<功能>_<状态>.png\` | 最大边 512，保留按钮比例 | \`close.png\`（缺前缀和状态） |
| \`frames/\` | 对话框外框、Tooltip 框、九宫格切片框 | \`frame_<用途>.png\` | 最大边 512，保留外框比例 | 与 \`lists/\` 混淆 |
| \`icons/\` | 功能图标、道具图标、货币图标、头像 | \`icon_<对象>[_<尺寸>].png\` | 最大边 64/128/256 | 不加尺寸后缀的多版本 |
| \`lists/\` | 列表行背景、商品卡、技能卡底图 | \`card_<用途>.png\` 或 \`list_<用途>.png\` | 最大边 512，保留卡片比例 | 与 \`frames/\` 混淆 |
| \`decorations/\` | 光效、粒子贴图、花纹、角标（**非交互**） | \`deco_<描述>.png\` | 最大边 512，保留插画比例 | 把按钮放这里 |
| \`text/\` | 艺术字标题、Logo、装饰性大数字 | \`title_<场景>.png\` 或 \`logo_<品牌>.png\` | 最大边 512，保留文字比例 | 普通文字做成图（应用 Label 渲染） |
| \`misc/\` | 暂时无法归类的素材（应急缓冲区） | 自由命名，仍需小写+下划线 | 最大边 512，保留比例 | 长期不清理 |

> \`frames/\` 是「外壳」，\`lists/\` 是「行/卡片内容底图」—— 容易混淆，按是否包内容判断。

### 4.4 命名标准（强制）

\`\`\`
<分类前缀>_<语义名>[_<尺寸或状态>].png
\`\`\`

| 规则 | 说明 | ✅ 正确 | ❌ 错误 |
|---|---|---|---|
| 全小写 | 禁止大写字母 | \`icon_coin.png\` | \`Icon_Coin.png\` |
| 下划线分词 | 禁止空格、连字符、中文 | \`btn_confirm_normal.png\` | \`btn-confirm normal.png\`、\`按钮.png\` |
| 前缀=分类 | 必须以分类名开头 | \`icons/icon_coin.png\` | \`icons/coin.png\` |
| 尺寸后缀 | 多尺寸版本必加 | \`icon_coin_256.png\` | \`icon_coin_big.png\` |
| 按钮状态 | 按钮必加状态后缀 | \`btn_confirm_pressed.png\` | \`btn_confirm_2.png\` |

---

## 五、标准加工流程（去绿幕 → 裁边 → 压缩）

\`脚本区/\` 提供了配套工具脚本，对应下面三个标准步骤。每批 AI 生图或截图都应走完整流程再进成品目录。

### 5.1 四步流程

\`\`\`
原始素材/  ──[1.去绿幕]──▶  临时文件/去绿幕后/  ──[2.裁边+压缩+规范化]──▶  临时文件/待审核/<分类>/  ──[3.人工审批]──▶  成品素材/<分类>/
\`\`\`

> **重要**：第 2 步脚本的 \`--output-dir\` 必须指向 \`临时文件/待审核/<分类>/\`，**禁止直接写入 \`成品素材/\`**。
> 所有素材必须经过第 3 步人工审批后才能进入成品目录。审批是直接在 UI 工作区目录里人工操作（见第 3 步说明），编辑器没有专门的审批界面。

#### 第 1 步：去绿幕（\`green_key_to_png.py\`）

AI 生图或拍摄图常带绿幕背景，必须先抠出前景。

\`\`\`bash
cd 脚本区
python green_key_to_png.py \\
    --input-dir  ../原始素材/2026-06-28/batch01-绿幕 \\
    --output-dir ../临时文件/去绿幕后
\`\`\`

- HSV 绿色判定 + 边缘扩散 + BFS 连通边缘溢出清除
- 自动去掉文件名中的「绿幕」「green」等标记
- 不改变尺寸，只去背景

#### 第 2 步：裁边 + 压缩 + 尺寸规范化（\`trim_compress.py\`）

把去绿幕后的图规范成符合 §三、§四 的待审核素材。默认 alpha 紧裁后按分类最大边等比缩小，不会补成正方形画布。

\`\`\`bash
# 输出到待审核目录（推荐，必须人工审批后才能进成品素材）
python trim_compress.py \\
    --input-dir  ../临时文件/去绿幕后 \\
    --output-dir ../临时文件/待审核/icons

# 或显式指定分类
python trim_compress.py \\
    --input-dir  ../临时文件/去绿幕后 \\
    --output-dir ../临时文件/待审核/buttons \\
    --category buttons

# 只有明确需要固定画布时，才指定宽高与 padding
python trim_compress.py \\
    --input-dir  ../临时文件/去绿幕后 \\
    --output-dir ../临时文件/待审核/backgrounds \\
    --width 1024 --height 512 --padding 0
\`\`\`

这一步会自动：
- alpha 裁边（去掉四周透明像素）
- 默认保留原始宽高比，仅限制分类最大边
- 仅在显式传入 \`--width\` / \`--height\` 时居中适配固定画布
- PNG 量化压缩（默认 192 色）
- 输出文件名规范化（需要多尺寸版本时再加尺寸后缀）

> **禁止将 \`--output-dir\` 直接指向 \`成品素材/\`**。所有脚本加工的素材必须先进 \`临时文件/待审核/\`。

#### 第 3 步：人工审批（直接在工作区目录操作）

编辑器没有审批面板，审批就是在 UI 工作区目录里直接操作 \`临时文件/待审核/<分类>/\` 中的文件：
- 用资源管理器 / 看图工具逐张查看，确认透明边缘干净（无绿边残留）
- 确认尺寸符合 §三 表格
- 确认文件名符合 §4.4 命名标准
- **批准**：把文件移动到 \`成品素材/<分类>/\`（保持分类子目录），之后即可在编辑器和发布中使用
- **拒绝**：删除该文件（不合格的请回到第 1 步重新加工）

### 5.2 脚本区维护

\`脚本区/\` 的脚本由 DJUI 编辑器统一维护。当 DJUI 仓库的 \`scripts/\` 升级后，编辑器会通过「检查工作区更新」提示同步到工作区的 \`脚本区/\`。
**不要**在工作区里手动改脚本，定制需求请改 DJUI 仓库 \`scripts/\` 的源码。

脚本版本号在工作区 \`脚本区/version.txt\`，与 DJUI 仓库 \`scripts/version.txt\` 比较。

---

## 六、完整工作流程

1. 将原始素材或 AI 生图放入 \`原始素材/YYYY-MM-DD/\`
2. 按 §五 四步流程加工：去绿幕 → 裁边+压缩+规范化 → 人工审批 → 进入成品素材
3. 第 2 步输出到 \`临时文件/待审核/<分类>/\`，第 3 步人工审阅后手动移入 \`成品素材/<分类>/\`
4. 在 DJUI 编辑器中编辑 UI 布局
5. 全屏背景按 §3.4 出竖版 / 宽版两套，页面里基础层挂竖版、宽屏层挂宽版
6. **直接改页面 JSON 的，改完必须跑本地校验（见 §6.1），通过才算改完**
7. 点击「发布」，\`成品素材/\` 全部内容（含子目录）原样同步到星火工程的 \`ui/image/djui/\`（发布前编辑器会自动跑同一套校验，有问题会拒绝并提示）
8. 引擎中通过 \`image/djui/<分类>/<文件名>\` 引用

### 6.1 本地校验（改完 JSON 必跑）

无论人改还是脚本/AI 改 \`.djui/layout/pages/*.json\` 或 \`.djui/layout/project.json\`，保存后执行：

\`\`\`bash
# 在 DJUI 仓库根目录(需 Node.js,无其他依赖)
node scripts/validate-pages.mjs <星火工程根目录>
# 或等价: cd <星火工程根目录> && node D:/git/DJUI/scripts/validate-pages.mjs
\`\`\`

输出「全部通过 ✓」(退出码 0)才算改完；列出任何 ✗ 都必须先修复。校验规则与编辑器完全一致：协议版本、节点结构、safe 锚点声明、cover/contain 的 sourceSize、响应式覆盖的节点 ID 引用与字段封闭列表、音效引用存在性。

### 6.2 AI / 命令行发布

AI 在工作区完成素材或页面修改后，可以不打开网页，直接调用 \`脚本区/djui-publish.mjs\`。发布规则与网页发布共用同一核心，**不要**手工复制文件或另写发布脚本。

首次由 AI 向用户确认星火工程目录后执行：

\`\`\`powershell
node .\\脚本区\\djui-publish.mjs configure --star-project "D:\\git\\MyStarProject" --json
\`\`\`

后续发布：

\`\`\`powershell
node .\\脚本区\\djui-publish.mjs publish --json
\`\`\`

- 目标路径保存在 \`.djui/publish.json\`；若 CLI 返回 \`MISSING_TARGET_CONFIG\`，AI 必须停止并向用户索取路径，再执行 \`configure\`
- CLI 返回 JSON 和退出码，AI 应根据 \`ok\`、\`code\`、\`userAction\` 处理；不得忽略失败继续声称已发布
- \`publish\` 会严格镜像资源和页面，目标侧已不在工作区的旧文件会被删除
- Runtime 缺失或过期时，\`publish\` 会返回 \`RUNTIME_NOT_READY\`（退出码 20）并阻止发布；AI 必须询问用户「是否允许更新 Runtime」，得到明确同意后才可执行 \`node .\\脚本区\\djui-publish.mjs upgrade-runtime --json\`，再重新发布
- 若返回 \`PUBLISHER_OUTDATED\`（退出码 25），说明本地发布器比星火工程已装的 Runtime 旧（通常发生在用户已从网页更新 Runtime 之后）；此时**禁止**执行 \`upgrade-runtime\`（会把 Runtime 降级并损坏工程），必须停止发布，请用户在 DJUI 网页执行「检查工作区更新」同步脚本区后重试
- 若返回 \`INVALID_WORKSPACE\` 且 error 提示「页面不是 v6 协议」，说明该页面缺 \`protocolVersion: 6\`，发布器拒绝自动迁移改写；必须请用户在 DJUI 编辑器打开该页面并保存（自动转为 v6）后重试。**禁止**手工给页面补版本号或改动协议字段冒充 v6
- \`upgrade-runtime\` 与 \`publish\` 必须分开调用；不得自动覆盖星火工程的 Runtime
- 当编辑器提示更新脚本区并执行同步时，会自动把旧星火工程内的布局源迁入工作区；迁移只在工作区尚无 \`.djui/layout/project.json\` 时执行，绝不会覆盖现有编辑源

---

## 七、发布行为

- 发布会将工作区 \`.djui/layout/\` 的项目配置、页面和音效镜像到星火工程；游戏工程内副本不是编辑源
- 发布会**完整覆盖** \`ui/image/djui/\` 下同名素材文件
- 子目录结构原样保留：\`成品素材/icons/abc.png\` → \`ui/image/djui/icons/abc.png\`
- \`原始素材/\` \`临时文件/\` \`脚本区/\` **不会**被发布
- 发布前请关闭引擎对相关资源的占用（否则 Windows 可能锁文件）

---

## 八、字体使用规范

字体也是 UI 资产的一部分，但管理与图片不同：

- **默认不选字体 = 引擎默认字体**（星火内置字体，无需提供任何文件）。除非确有美术需求，保持默认即可
- 需要特色字体时，在 DJUI 编辑器的字体下拉 → 「导入新字体」导入**标准字体文件**（.ttf / .otf / .ttc）
- 只有标准字体文件能同时被编辑器画布和游戏引擎加载——导入后两端渲染**完全一致**；微软雅黑等系统字体两端直接调用操作系统字体，同样一致
- 星火自带的字体文件是引擎私有封装格式，画布只能近似预览，**不要**把它们当作可复用素材拷贝搬运
- **字体授权**：只导入拥有商用授权的字体（如思源黑体、霞鹜文楷等开源字体）；不要把 Windows 系统字体导入游戏工程用于发行
- 删除字体只能删通过 DJUI 导入的，引擎自带字体不可删

---

## 九、版本

- 编辑器内置 AGENTS 规范版本：\`\${AGENTS_VERSION}\`
- 当此处版本号 > 工作区本文件顶部版本号时，编辑器会提示更新
- 更新会**整文件覆盖**（你的素材目录不受影响，只是这份说明文档被刷新）

> 此文件由 DJUI Editor 自动生成。
`
}
